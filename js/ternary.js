let age = 11;
// let type;

// if (age >= 18) {
//     type = 'adult';
// }
// else {
//     type = 'child';
// }

//let type = (age >= 18) ? 'adult' : 'child';

let type = (age >= 18) ? 'adult' : (age < 10) ? 'child' : 'teen';

//console.log(type);

let a = 3;

//let access = a > 5 ? true : false;
let access = a > 5;

console.log(access);